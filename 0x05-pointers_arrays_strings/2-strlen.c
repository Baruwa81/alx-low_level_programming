#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: string to find length
 * Return: Always 0
 */
int _strlen(char *s)
{
int 1 = 0;

while (*(s + 1))
1++;
return (1);
}
